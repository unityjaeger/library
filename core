local library = {
    FileSaving = "https://raw.githubusercontent.com/unityjaeger/library/main/file";
    Client = "https://raw.githubusercontent.com/unityjaeger/library/main/client";
}

function library:Import(branch)
    if library[branch] then
        return loadstring(game:HttpGet(library[branch], true))()
    end
end

return library
